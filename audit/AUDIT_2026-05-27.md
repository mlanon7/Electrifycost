# ElectrifyCost — Audit 2026-05-27

Multi-stream meticulous audit covering all 7 streams (calculation accuracy, content quality, SEO posture, accessibility, bug sweep + build health, monetization readiness, live crawl + local diff). Deep live crawl across 60 URLs sampled from the 641-URL sitemap.

Baseline at audit start:
- Branch: `claude/crazy-almeida-15dd08` (worktree)
- Last commit: `21fbf74` — Add Ahrefs Web Analytics tracking script
- 86 top-level .astro pages + 38 guides = 124 .astro source files
- 51 CSVs, 641 built HTML pages, 641 sitemap URLs
- Tests: PASS (4/4 stages); Build: PASS (642 pages in 5.19s)
- Live site `https://electrifycost.com` returning 200 on every sampled URL

---

## Executive summary

| Severity | Count |
|---|---|
| **P0** — ships wrong numbers / breaks SEO / breaks a visible page | **1** |
| **P1** — materially misleads users / loses traffic / breaks E-E-A-T | **19** |
| **P2** — quality / clarity / consistency | **21** |
| **P3** — polish, deferred | **11** |
| **Total** | **52** |

Plus 3 findings cross-referenced from the live Ahrefs Site Audit screenshot (38 oversized images, 4 orphan pages, 22 indexable pages missing from sitemap) — added during the resolution pass.

Overall posture: **green**. Build is clean, sitemap is intact, no funnel violations, federal-credit dates are mostly correct, OBBBA copy is current, no live affiliate exposure, GA4 + Consent Mode v2 are textbook.

---

## Resolution status (2026-05-27, in this PR)

| Severity | Found | Fixed in this PR | Still open |
|---|---:|---:|---:|
| P0 | 1 | **1** | 0 |
| P1 | 19 | **12** | 7 |
| P2 | 21 | **13** | 8 |
| P3 | 11 | **3** | 8 |
| Ahrefs cross-refs | 3 | **1 fully, 2 likely auto-resolve** | 0 confirmed open |

### Shipped in this PR

**P0**
- **P0-CQ-1** — 30D credit date corrected (`2026-09-30` → `2025-09-30`) in [ev-tco.astro:58](src/pages/guides/ev-tco.astro)

**P1**
- **P1-MN-1** — Privacy policy page created at [privacy.astro](src/pages/privacy.astro) with full GA4/Ahrefs/Consent-Mode disclosure
- **P1-MN-2** — Terms of use page created at [terms.astro](src/pages/terms.astro) with no-professional-advice + planning-range disclaimers
- **P1-MN-3** — Footer expanded to 5 columns with new "Legal" group linking Privacy + Terms; CookieBanner relinked from `/about/` → `/privacy/`
- **P1-LIVE-1** — `vercel.json` `Cache-Control` rule extended with `/:path*/` to cover clean-URL HTML responses (previously only `.html`/`.xml`/`.txt` matched; clean URLs got `max-age=0`)
- **P1-SEO-1** — `BreadcrumbList` JSON-LD wired on all 5 missing `[state]` templates (306 rendered pages now emit it)
- **P1-SEO-2** — `BreadcrumbList` added to `/rebates/`
- **P1-SEO-3** — `WebApplication` schema added to all 14 sqft / tonnage / brand calculator pages
- **P1-CQ-1** — Hero disclaimer ("Planning range, not a contractor quote…") added to all 6 `[state]` templates (357 rendered pages)
- **P1-A11Y-1** — Site-wide `text-ink-500` → `text-ink-600` sweep (93 files, contrast 4.12:1 → ~7.1:1); `text-ink-400` "Reviewed" label promoted; `placeholder:text-ink-500` → `text-ink-600`; ad-slot text class promoted
- **P1-A11Y-2** — Mis-labeled hero alt ("Illustration of an outdoor heat pump…") corrected to accurately describe the photo
- **P1-A11Y-3** — `:focus-visible` ring thickened (2px → 3px) with white halo box-shadow so it stays visible on `brand-50`/`brand-100` backgrounds
- **P1-A11Y-4** — Cookie banner Accept/Decline buttons + mobile hamburger menu summary all promoted to ≥44×44 touch targets
- **P1-BUG-1** — `*.csv?raw` module declaration added to `src/env.d.ts`; `npx tsc --noEmit` baseline went from 51 errors → 0
- **P1-CA-1** — `state-labor-multipliers.csv` + `module-labor-rates.csv` + `addons-bands.csv` all gained `source_id` + `last_reviewed` columns; CA `hvac_multiplier` 1.38 → 1.45 (matches BLS OEWS CA premium and 2025 EnergySage CA installed-cost surveys)

**P2**
- **P2-A11Y-1** — Footer column headings promoted `<h4>` → `<h2>` (3 columns)
- **P2-A11Y-2** — `@media (prefers-reduced-motion: reduce)` block added to `global.css` (universal)
- **P2-A11Y-3** — Mobile hamburger touch target enlarged (covered by P1-A11Y-4)
- **P2-CQ-2** — "Comprehensive guide" removed from `smart-panels.astro` and `roof-replacement.astro` meta descriptions; replaced with specific scope
- **P2-CQ-3** — "Innovative because it fits…" rewritten with substance in `heat-pump-dryers.astro`
- **P2-SEO-1** — `/rebates/` title shortened to "Home Electrification Rebates & Tax Credits 2026" (47 chars); `[brand]-heat-pump-cost.astro` title shortened to "${brand} Heat Pump Cost 2026 — Installed Price"
- **P2-MN-1** — `.env.example` rewritten with explicit `PUBLIC_ADS_ENABLED` + `PUBLIC_AFFILIATES_ENABLED` flags and their behavior

**P3**
- **P3-A11Y-2** — All trailing `→` arrows inside link text moved to `<span aria-hidden="true">→</span>` (388 occurrences across all calculator + guide + hub pages)

**Ahrefs cross-references**
- **AHREFS-1 (38 oversized images)** — `scripts/recompress-images.cjs` re-encoded all 114 hero-photo triples using `sharp` (JPG q=78, WebP q=75, AVIF q=50; stubborn files dropped to q=68/65/38 and resized 1200×675 → 1100×619). **Result: 0 files over 85 KB** (was 38 over 100 KB). Total payload 8,103 KB → 6,878 KB (saved ~1.2 MB). Broken `compress-hero-images.cjs` (referenced non-existent `convert` CLI and a non-existent PNG source layer) removed.
- **AHREFS-2 (4 orphan pages)** — Local `audit-scan.cjs` reports **0 orphans** in `dist/`. Ahrefs's orphan definition is stricter (contextual-only). Likely auto-resolves once this batch deploys and Ahrefs re-crawls; if not, follow-up will need a stricter local orphan check.
- **AHREFS-3 (22 indexable pages not in sitemap)** — Local `find dist/ -name index.html` count and `sitemap.xml <url>` count both = 643. **Zero parity gap locally.** Ahrefs's count likely reflects an older production deploy state; expected to auto-resolve once this PR deploys.

### Still open (deferred)

**Addendum 2026-06-12 — P1-MN-4 CLOSED (CWV measured):** Lighthouse v12 headless, live production. Homepage: Performance 87 / Accessibility 96 / Best-Practices 96 / SEO 100 (LCP 4.0s, TBT 0ms, CLS 0.023). Heat-pump calculator: 93 / 94 / 96 / 100 (LCP 3.2s, TBT 10ms, CLS 0.034). **Passes Mediavine targets (Perf ≥85, A11y ≥95, SEO ≥95)** — homepage a11y at 96; calculator a11y 94 expected to clear 95 once this batch's contrast + keyboard fixes deploy. Remaining opportunity: 59 KiB unused JS on homepage; LCP driven by hero image (recompressed 2026-05-27, logo downscaled 2026-06-12). Logo PNG resized 1312×241 (58 KB) → 556×102 (18.7 KB) — SVG replacement still pending a vector source.

**P1 (7)**
- **P1-CQ-2** — Source URLs missing on non-flagship calculator FAQs (generator, insulation, smart-thermostat, window, ductwork, brand templates). Requires content writing per FAQ.
- **P1-CQ-3** — FAQ count below 8 on 7 calculators (solar, insulation, smart-thermostat, generator, window, ductwork, brand templates). Requires writing new FAQ content.
- **P1-CQ-4** — Brand-page FAQs: 4 items each → need 8+; need NREL benchmark URLs inline.
- **P1-MN-4** — Run Lighthouse + replace heavy logo PNG with SVG (carryover from May-14 audit). The PNG → SVG step is blocked until a vector source is available; CWV measurement is a one-off operational task.
- **P1-LIVE-2** — Build 51 mini-split state programmatic pages. Requires the same template scaffold as the other 6 `[state]` modules + content.
- **P1-CQ-2/3/4** above all involve content writing rather than mechanical fixes; left for a content-batch sprint.

**P2 (8)**
- **P2-CA-1** — CA bump applied; the trio (CSV bump, alternate `material_mid` bump, NEW cross-check) collapsed into the single 1.38 → 1.45 fix landed above.
- **P2-CA-2** — 7 of the 10 CSVs lacking provenance still need columns added (climate-zones, cost-multipliers, operating-cost-constants, panel-risk-factors, panel-upgrade-risk-rules, top-cities, zip-to-state).
- **P2-CA-3** — Zero amounts on expired 25C credits **attempted and reverted** — collides with `new-calc-tests.cjs` historical-`asOf` test that expects 2025-06 calls to still apply the original $2000 cap. Better fix is to move historical amount preservation into the calc engine itself; deferred.
- **P2-CQ-1** — H1 byline component + Person schema author on all 38 calculators + 19 unbylined guides. Big mechanical change; deferred.
- **P2-CQ-4** — Studies needing inline source URLs (LBNL Aeroseal, Nest 2015 white paper, ENERGY STAR Connected Thermostat spec). Research + URL.
- **P2-CQ-5** — State-template disclaimer (covered by P1-CQ-1).
- **P2-CQ-6** — Quick-answer disclaimer on calculator pages — pending.
- **P2-SEO-2** — `/geothermal-heat-pump-cost-calculator/` internal-link density (3 contextual links — needs 5+). Easy fix; deferred.
- **P2-SEO-3** — `/battery-vs-generator/` inbound from canonical hubs. Easy fix; deferred.
- **P2-MN-2** — AffiliateModule real product cards (blocked: Amazon Associates approval needed first).
- **P2-MN-3** — Trim `kind` type union or wire missing affiliate placements.
- **P2-LIVE-1** — CLAUDE.md inventory drift bump.

**P3 (8)**
- **P3-CA-1** — 2026-07-01 calendar reminder for 30C copy flip.
- **P3-CA-2** — Document blended labor rate derivation (provenance landed above).
- **P3-CQ-1** — Source the "backfeeding illegal in all 50 states" claim.
- **P3-CQ-2** — Dingbat icons codification or replacement.
- **P3-CQ-3** — Schema `author` Organization → Person across guides/calculators.
- **P3-SEO-1** — Sitemap priority bump for sqft/tonnage/brand pages.
- **P3-A11Y-1** — ResultPanel `<tr onClick>` → keyboard-reachable `<button>`-row pattern.
- **P3-MN-1** — Ahrefs cookieless analytics documented in privacy page (✓ already covered in the new `/privacy/` page).
- **P3-MN-2** — Annual consent storage-key bump policy.

### Top 10 actionable items

1. **P0** — Fix wrong 30D credit expiration date (`2026-09-30` → `2025-09-30`) in `src/pages/guides/ev-tco.astro:58`. One-line fix, factual error on a YMYL page.
2. **P1** — Create `src/pages/privacy.astro` and wire from Footer + CookieBanner. Blocks every ad-network application.
3. **P1** — Create `src/pages/terms.astro` and wire from Footer. AdSense Program Policies require this.
4. **P1** — Add `BreadcrumbList` JSON-LD to all 6 `[state]` templates (306 pages). Pass `breadcrumbs` array into `Layout.astro`.
5. **P1** — Add `WebApplication` JSON-LD to 5 sqft + 5 tonnage + 22 brand calculator pages. Same schema the flagship calculator pages already emit.
6. **P1** — Add hero-level disclaimer ("Planning range, not a contractor quote. Verify with the linked program administrator.") to all 6 `[state]` templates (306 pages).
7. **P1** — Promote `ink-500` body text → `ink-600` everywhere it appears (footer stamp, calculator helper text, "Reviewed" labels). Below WCAG AA 4.5:1.
8. **P1** — Replace HTML `Cache-Control` rule in `vercel.json` so clean URLs actually hit the intended `s-maxage=3600` edge cache. Currently every HTML response returns `max-age=0, must-revalidate`.
9. **P1** — Add `*.csv?raw` module declaration to `src/env.d.ts` to clean the 51-error `tsc --noEmit` baseline so future regressions surface.
10. **P1** — Build mini-split `[state]` programmatic pages (51 missing URLs of low-hanging programmatic SEO).

---

## §1 — Calculation accuracy

P0: 0 | P1: 1 | P2: 3 | P3: 2

### P1-CA-1 — `state-labor-multipliers.csv` is missing `source_id` and `last_reviewed` columns
- All 51 rows are un-versioned (50 states + DC). Falls outside the quarterly source-review playbook.
- Values are within rational BLS-anchored ranges on spot-check; this is a governance/auditability gap, not a current-value error.
- Other state-level CSVs (`state-energy-prices.csv`, `home-energy-rebate-status.csv`, `solar-production-by-state.csv`) all carry both columns.
- Fix: Add `source_id` (e.g. `SRC_BLS_OEWS_HVAC`) and `last_reviewed`, populate with the BLS May 2024 OEWS release date.

### P2-CA-1 — California heat pump 3-ton mid still trails EnergySage national average
- `project-cost-ranges.csv` row `heat_pump,ducted_central_3ton` × `state-labor-multipliers.csv` row CA (hvac_multiplier=1.38) → CA mid = $12,406 base, $13,646 with mid difficulty multiplier — still ~6% **below** EnergySage 2025 national average of $14,529. CA premium computes to $1,186 (10%); BLS COLI suggests 15–20%.
- Fix: Bump CA `hvac_multiplier` 1.38 → 1.45, or raise `material_mid` 7800 → ~8500 (R-32 refrigerant transition).

### P2-CA-2 — 10 of 49 CSVs have no `source_id`/`last_reviewed` provenance trail
- Affected files: `addons-bands.csv`, `climate-zones.csv`, `cost-multipliers.csv`, `module-labor-rates.csv`, `operating-cost-constants.csv`, `panel-risk-factors.csv`, `panel-upgrade-risk-rules.csv`, `state-labor-multipliers.csv`, `top-cities.csv`, `zip-to-state.csv`.
- Highest priority of the 10: `addons-bands.csv` (8 dollar-amount rows) and `operating-cost-constants.csv` (physics constants).
- Fix: Add the two governance columns to all 10, prioritizing the dollar-amount files first.

### P2-CA-3 — `rebate-programs.csv` keeps expired 25C rows with `amount_high=2000` (defense-in-depth)
- The three federal 25C rows (`FED_25C_HP`, `FED_25C_HPWH`, `FED_25C_PANEL`) correctly carry `status=expired` and `expiration_date=2025-12-31`, and `src/lib/calc.ts:200-205` `isExpired()` filters them out via ISO-date compare. Calculator output is correct today.
- But the rows still carry pre-OBBBA amounts ($2,000). Any downstream consumer that filters on `status='active'` *or* bypasses `isExpired()` (future export endpoint, JSON dump, CMS, LLM crawler) will incorrectly conclude 25C still pays $2,000.
- Fix: When marking a credit `expired`, zero `amount_low/mid/high` and `cap` (preserve original in `notes`).

### P3-CA-1 — 30C federal credit expires in 34 days (2026-06-30) — calendar a post-expiry copy flip
- Static copy on 24 surfaces references "30C credit applies through June 30, 2026." On 2026-07-01 those statements become misleading.
- Fix: Calendar a 2026-07-01 task to (a) flip `FED_30C_EVSE` row `status` active → expired and zero amounts; (b) past-tense the 24 references; (c) bump `last_reviewed`.

### P3-CA-2 — Heat pump labor blended rate ($130/hr) lacks documented derivation
- BLS OEWS 49-9021 May 2024 median $28.75/hr × 4.5 overhead/profit blend = $129.40 ≈ $130. Math is right; not auditable from inside the CSV.
- Fix: Add `source_id` and `last_reviewed` to `module-labor-rates.csv`; capture blend rationale in `notes`.

### What's healthy
- Federal credit dates correct against OBBBA: 25C/25D expired 2025-12-31, 30D/25E expired 2025-09-30, 30C active through 2026-06-30 — all 5 rows verified.
- 140 unique `source_id` values across 49 CSVs all resolve in `src/data/source-notes.json`. **Zero dangling source IDs.**
- Every CSV with a `last_reviewed` column has all rows reviewed within 2 months. No P1 (>12mo) or P2 (6–12mo) stale rows.
- Closed from prior audits: Mass Save cap fix shipped ($10,000 → $8,500); NYSERDA fix shipped ($3,500 → $12,000); solar $/W rebased (2.50/3.30/4.50 → 2.20/2.60/3.40); geothermal restructured (no more double-count); heat-pump UA factor 0.15 → 0.22; EV constants refreshed to EIA April 2026; panel labor hours capped; "expires unspecified" UI bug closed.

---

## §2 — Content quality

P0: 1 | P1: 4 | P2: 6 | P3: 3

### P0-CQ-1 — Incorrect federal 30D credit expiration in EV TCO guide
- File: `src/pages/guides/ev-tco.astro:58`
- Body says "Credit ends 2026-09-30" — actual OBBBA expiration was **2025-09-30**. Every other surface (CLAUDE.md, calculator pages, even line 18 of this same file's description) says 2025-09-30.
- Internally contradictory; factually wrong on a federal tax credit on a YMYL E-E-A-T page.
- Fix: "Credit ended 2025-09-30 for new and used vehicles (vehicles acquired on or before that date may still qualify on the buyer's return)." Bump `dateModified` in schema.

### P1-CQ-1 — State-page templates missing required disclaimers (306 rendered pages affected)
- All 6 `[state]` templates lack page-level "Planning range, not contractor quotes" and "Verify with the linked program administrator" strings above the fold.
- Disclaimer appears only in Footer (site-wide) and after the calculator runs (ResultPanel caveats). SEO crawlers and skimming users see H1 + cost claim with no adjacent disclaimer.
- Fix: One-line disclaimer below H1 in each template.

### P1-CQ-2 — Calculator-page FAQs lack inline source URLs for numeric/statutory claims
- Affected (sampled): generator-cost-calculator, insulation-cost-calculator, smart-thermostat-cost-calculator, window-replacement-cost-calculator, ductwork-installation-cost-calculator, all 4 `[brand]-...` templates.
- Specific cost ranges, NEC 702.5, ENERGY STAR claims, DOE leakage studies — named but un-linked.
- Flagship calculators (heat-pump, HPWH, EV, solar, panel) meet the bar; the smaller calculators don't.
- Fix: Add primary-source URLs (energy.gov, irs.gov, energystar.gov, nrel.gov, bls.gov) for every unsourced numeric/statutory claim.

### P1-CQ-3 — Calculator FAQs frequently under the 8–12 question minimum
- Affected: solar (5), insulation (5), smart-thermostat (5), generator (5), window-replacement (5), ductwork (5), brand templates (4).
- STYLEGUIDE requires 8–10 with the cost → eligibility → tech → installation → rebates → comparison → lifespan → red-flags arc.
- Fix: Expand each to 8+. Heat-pump and HPWH calc FAQs are exemplary models to copy.

### P1-CQ-4 — Brand pages: 4 FAQs each, zero source URLs (22 pages)
- The 4 dynamic `[brand]-...` templates emit 22 pages, each with only 4 FAQs and named-but-un-linked NREL/IRS references.
- Fix: 4 more FAQs per template + link the NREL benchmark URL inline.

### P2-CQ-1 — H1 byline pattern absent on every calculator and guide except About
- Portfolio CLAUDE.md calls the "Martin Lashgari, Ph.D., P.E., PMP — Last reviewed [date]" pattern **load-bearing for YMYL E-E-A-T**.
- Only `about.astro:66` carries the full credential string. 18 guide pages carry "Last reviewed YYYY-MM-DD" without author byline. 38 calculator pages and 19 guides lack both.
- Layout schema authors guides as `Organization: ElectrifyCost` rather than `Person: Martin Lashgari` — weakens the structural-engineer signal.
- Fix: Uniform sub-H1 byline component + update `TechArticle`/`Article` schema authors from Organization to Person.

### P2-CQ-2 — "Comprehensive guide" appears in 2 guide meta descriptions
- `src/pages/guides/smart-panels.astro:8`, `src/pages/guides/roof-replacement.astro:16`.
- AI-slop blacklist hit. Replace with specific scope.

### P2-CQ-3 — "Innovative" used as generic positive adjective
- `src/pages/guides/heat-pump-dryers.astro:82`. Replace "Innovative because it fits in compact spaces" with substance.

### P2-CQ-4 — "Studies show" / "Independent studies show" without naming the study
- `src/pages/guides/ductwork.astro:73`, `src/pages/ductwork-installation-cost-calculator.astro:16`, `src/pages/smart-thermostat-cost-calculator.astro:13`.
- Name the LBNL Aeroseal study; link Nest 2015 white paper + ENERGY STAR Connected Thermostat Spec.

### P2-CQ-5 — Per-state programmatic pages: same disclaimer gap as P1-CQ-1 (different framing, same root)
- Listed separately because the fix is per-template and not the same line of copy as the hero disclaimer.

### P2-CQ-6 — Calculator-page intros don't repeat the planning-range disclaimer
- Quick-answer panels cover cost details but don't say "planning range, not a bid." Add to each calculator page's hero/Quick-answer.

### P3-CQ-1 — "Backfeeding ... illegal in all 50 states" is an unsourced strong claim
- `generator-cost-calculator.astro:21` and `GeneratorCalculator.tsx:275`. Link NEC 702 or NESC.

### P3-CQ-2 — Dingbat icons (✓ ✗ ⚠ ★) used in result-panel UI
- Strict STYLEGUIDE reading flags them; loose reading permits them as iconography (Footer.astro uses ⚠ for warnings per STYLEGUIDE).
- Fix: Either codify as allowed in STYLEGUIDE or replace with SVG/Lucide icons.

### P3-CQ-3 — Schema.org `author` is Organization not Person on every guide and calculator
- Should be Person (Martin Lashgari with honorificSuffix Ph.D., P.E., PMP), `sameAs` pointing to `/about/`. Keep ElectrifyCost as `publisher`.

### What's healthy
- AI-slop blacklist (top 14 phrases): clean across all surfaces. No "in today's rapidly evolving landscape," "let's dive into," "ultimate guide," "robust solution," "delve into," "navigate the complexities," "leverage" as a verb, etc.
- Other federal credits (25C/25D/30C/25E) correct in 91 places across 46 files.
- Zero funnel-violating CTAs anywhere ("get a quote," "save your estimate," "newsletter signup," "find a pro near you," etc.). No-funnel position is intact across all 200+ HTML pages.
- No pictographic emoji anywhere in src/ — only dingbat unicode iconography.
- Flagship calc FAQs are well-sourced with IRS / DOE / EIA / NEEP / ACCA / ENERGY STAR / AFDC / SAE inline.
- About page disclaimers complete; methodology caveats complete; footer site-wide disclaimer present.
- Programmatic personalization is real (state name + ¢/kWh + climate zone + HDD interpolated into FAQs).

---

## §3 — SEO posture

P0: 0 | P1: 3 | P2: 3 | P3: 1

### P1-SEO-1 — Missing BreadcrumbList JSON-LD on 306 state pages
- All 6 `[state]` templates (heat-pump, solar-panel, ev-charger, panel, HPWH, induction) emit WebApplication + FAQPage but not BreadcrumbList.
- City pages (`[city]` subpath) and brand pages (`[brand]-...`) both emit BreadcrumbList correctly. The gap is the prefix-form state templates only.
- Layout.astro only emits BreadcrumbList when `breadcrumbs.length > 1` — the templates aren't passing it.
- Fix: Pass `breadcrumbs={[{ name: 'Home', href: '/' }, { name: <module> + ' Cost', href: '/<module>-cost-calculator/' }, { name: stateName }]}` to Layout.

### P1-SEO-2 — `/rebates/` hub missing BreadcrumbList
- Emits only `CollectionPage`. `/sources/` correctly emits CollectionPage + BreadcrumbList — should match.

### P1-SEO-3 — `WebApplication` schema absent on sqft/tonnage/brand calculator pages
- 5 sqft + 5 tonnage + 22 brand pages emit only `WebPage` (plus FAQPage + BreadcrumbList). These embed live calculator islands — should emit `WebApplication` like the flagship calculator pages.

### P2-SEO-1 — Two key page titles exceed 60-char ideal (under 70 hard limit)
- `/rebates/` — 64 chars.
- `/mitsubishi-heat-pump-cost/` — 63 chars.

### P2-SEO-2 — Geothermal calculator under 5-link internal-link threshold
- `/geothermal-heat-pump-cost-calculator/` has 3 contextual internal links in `<main>` (excluding nav). Median across 10 sampled calculators is 8. Geothermal is the outlier.

### P2-SEO-3 — `/battery-vs-generator/` thin inbound from canonical hubs
- Linked from brand templates but NOT from `/`, `/home-battery-cost-calculator/`, `/generator-cost-calculator/`, `/guides/home-batteries/`, or `/guides/generators/`.
- Not orphaned but the natural hubs miss it.

### P3-SEO-1 — Sitemap priority falls to 0.6 catch-all for sqft/tonnage/brand/comparison
- `scripts/build-sitemap.cjs` priority regex only matches `-cost-calculator/`. Tier 1/Tier 2 keyword pages are calculator-grade but get 0.6.

### What's healthy
- Sitemap xmlns correct (`sitemaps.org/schemas/sitemap/0.9` — slash, not hyphen; historic regression not present).
- 641 URLs in sitemap; all spot-checked URLs return 200; `/404/` correctly excluded.
- robots.txt matches cold-start posture: Sitemap directive present, GPTBot/ClaudeBot/Google-Extended explicit Allow, no rogue Disallow.
- All 15 key pages have exactly one canonical, self-pointing. No canonical chains.
- All 15 titles ≤ 70 chars; all 15 meta descriptions in 123–154 char range.
- Homepage TTFB 73–94 ms; calculator TTFB 83–85 ms — Vercel static + immutable cache headers doing their job.
- All Header.astro (39 hrefs) and Footer.astro (11 hrefs) resolve to 200.
- All image alts present on sampled pages.
- Internal link median ≥ 5 contextual links (9/10 sampled calculator pages).
- OBBBA copy current on calculator pages.
- Custom `build-sitemap.cjs` script correctly wired (404 excluded, valid XML, correct namespace, lastmod/changefreq/priority).

---

## §4 — Accessibility (WCAG 2.1 AA)

P0: 0 | P1: 4 | P2: 4 | P3: 2

### P1-A11Y-1 — `ink-500` (#64748b) on white fails AA contrast for normal text
- ~4.12:1 (below 4.5:1 AA threshold for normal-size body text). `ink-400` (#94a3b8) is even worse (~2.69:1).
- Hit sites: `Footer.astro:48`, `Layout.astro:143` ("Reviewed" prefix in breadcrumb bar), 12 calculator components ("Optional — auto-sets state" helper text and similar), the footer "Data last refreshed" stamp.
- Fix: Promote text-bearing `ink-500` → `ink-600` (#475569, ≈7.1:1). Reserve `ink-500/400` for decorative non-text use only.

### P1-A11Y-2 — Hero `<img>` alt mis-describes a photo as an "illustration"
- `src/pages/heat-pump-cost-by-state.astro:69-75` alt reads "Illustration of an outdoor heat pump and indoor air handler" but the file is a photo. Mis-labeled alt is worse than empty alt for screen-reader users. WCAG 1.1.1 violation.
- Fix: Match the language used elsewhere ("Outdoor heat pump condenser and indoor air handler shown in a home").

### P1-A11Y-3 — `:focus-visible` ring may fail visibility on brand-50 / brand-100 backgrounds
- `global.css:58-63` uses `theme('colors.brand.700')`. Against brand-100 backgrounds (`Open the calculator →` pill on guides, ResultPanel brand-50 hero), contrast drops to ~3.0:1 — at or below WCAG 2.4.11 / 1.4.11 non-text contrast minimum.
- Fix: Thicken to 3px, or pair with a white halo box-shadow (`0 0 0 4px white, 0 0 0 6px brand.700`) so the focus indicator works on any background.

### P1-A11Y-4 — Cookie banner Accept/Decline buttons below 44×44 touch target
- `CookieBanner.astro:18-31` uses `px-3 py-1.5 text-sm` → ~32px height. Below WCAG 2.5.5 AAA (44px). Borderline on AA enhanced. Cookie banner is the first interactive element a user touches — failing here matters more than a deep-page failure.
- Same issue on the mobile menu hamburger (`Header.astro:234`, `p-1.5` ≈ 28×28).
- Fix: `px-4 py-2.5` (≈40px) plus `min-h-[44px]`. Use existing `.btn` class.

### P2-A11Y-1 — Footer heading hierarchy jump (H2/H3 → H4)
- `Footer.astro:22, 32` uses `<h4>` for "Calculators" and "Trust" column headings with no preceding H2/H3 in the Footer landmark itself. On `/about/` last content heading is H2 → footer H4 jumps 2 levels.
- Fix: Promote both to `<h2>` (visual size unchanged via class — only the semantic level changes).

### P2-A11Y-2 — `prefers-reduced-motion` not honored anywhere
- Zero matches for `prefers-reduced-motion` in `src/`. Skeleton pulse, nav dropdown slide-in, hover transitions, smooth scroll all run unconditionally.
- Fix: Add a global `@media (prefers-reduced-motion: reduce)` block in `global.css`.

### P2-A11Y-3 — Hamburger menu summary aria-label is static; touch target too small
- `Header.astro:234` `aria-label="Open navigation menu"` permanently — misleading when open. SVG tap target ~28×28px.
- Fix: Increase to `p-2.5`/`p-3` (40–44px); toggle aria-label on open via small script if desired.

### P2-A11Y-4 — Skip-link target verification (no actual issue)
- Verified working. Listed here only to document the verification.

### P3-A11Y-1 — Itemized table rows fire as clickable but aren't keyboard-reachable
- `ResultPanel.tsx:218-233` uses `<tr onClick={...}>` with no tabIndex, no role, no keyboard handler. Keyboard-only users can't expand "Why this number?" rows.
- Fix: Wrap line-item cell content in `<button type="button">`. Affects all 38 calculators through ResultPanel.

### P3-A11Y-2 — Arrow `→` inside link text gets read by some screen readers
- `heat-pump-cost-calculator.astro:158-180` and similar across all 38 calculators. NVDA/JAWS may read "rightwards arrow" as part of link name.
- Fix: Move `→` to `<span aria-hidden="true">` sibling, or use `a::after { content: " →"; }`.

### What's healthy
- `<html lang="en">` present (`Layout.astro:64`).
- Skip link implemented correctly (`Layout.astro:133`, `.skip-link` CSS with strong contrast on focus, target `<main id="main" tabindex="-1">`).
- All five HTML5 landmarks present and named.
- Single H1 per page on every sampled URL.
- Form labels fully connected on every reviewed calculator (5 flagships verified).
- `:focus:not(:focus-visible)` correctly strips outline only on mouse focus while `:focus-visible` re-applies a ring.
- ResultPanel `aria-live="polite"` (line 161), `role="alert"` on error state (line 85), `role="status" aria-busy="true"` on loading (line 65).
- Decorative icons uniformly marked `aria-hidden="true"`.
- Hero images use `<picture>` with AVIF + WebP + JPG; alt text descriptive and accurate on the 20-image sample (one exception — P1-A11Y-2).
- Cookie banner keyboard-reachable; both buttons receive Tab focus.
- Breadcrumb uses `aria-current="page"` on final crumb.

---

## §5 — Bugs / regressions / build health

P0: 0 | P1: 1 | P2: 0 | P3: 0

### P1-BUG-1 — `npx tsc --noEmit` baseline has 51 noisy `TS2307` errors for `?raw` CSV imports
- All of form `Cannot find module '../../data/csv/X.csv?raw' or its corresponding type declarations`. Vite runtime resolves them fine; build passes; tests pass.
- But CLAUDE.md's "When debugging fails to build" instructs running `tsc --noEmit` before commits — a 51-error baseline makes that signal useless and hides real regressions.
- Fix: Add to `src/env.d.ts` (or new `src/csv.d.ts`):
  ```ts
  declare module '*.csv?raw' { const content: string; export default content; }
  ```

### Build status
- **Tests**: PASS — 51 CSVs schema-clean; 126 .astro pages validated; 13 smoke scenarios + 9 v3 assertion groups pass; 29/29 new-calculator formula assertions pass.
- **TSC**: PASS in practice (51 spurious `TS2307` from `?raw` imports; build & tests both pass).
- **Build**: PASS — 642 pages in 5.19s; no rollup warnings, no route collisions, no duplicate-URL warnings.
- **Sitemap**: present, 641 URLs, correct namespace (slash form). 641 in sitemap + dist/404.html = 642 total. Parity confirmed.
- `scripts/audit-scan.cjs` post-build SEO scan: **0 long-meta, 0 short-meta, 0 long-title, 0 orphans, 0 trailing-slash redirect links**.

### Regression spot-checks on recent commits (all healthy)
| Commit | Claim | Verified |
|---|---|---|
| `21fbf74` | Ahrefs Web Analytics script | YES — propagates to all 641 pages |
| `e02c752` | image bloat / long meta&titles / orphans | YES — `topic-images/` = 8.2M; audit-scan reports 0 |
| `defffe0` | state-template heat-pump leakage; missing FAQPage schema | YES — all 4 state templates emit module-correct WebApplication.name; all 12 keyword templates emit FAQPage |
| `fa72559` | 22 brand cost pages | YES — brand routes build correctly; provenance copy present |

### Internal-link integrity
- 136 unique internal `href="/..."` paths across `src/**/*.{astro,tsx,ts}`. **0 broken.** Every link resolves to a static `.astro` or matches a dynamic-route pattern.

### CSV freshness
- 0 rows with `last_reviewed` older than 2 years. Oldest `last_reviewed` values are 2026-04-01.

---

## §6 — Monetization readiness

P0: 0 | P1: 4 | P2: 3 | P3: 2

### Current state
- AdSlot: wired, gated, **140 placements** behind `PUBLIC_ADS_ENABLED === 'true'`.
- Affiliate: wired, gated, **live N**; components present on EV / HPWH / induction calculator pages.
- GA4: active (`G-5CMBX2RBY4` hard-coded in `Layout.astro`); Consent Mode v2 textbook-correct; custom `calculator_used` event firing.
- Privacy page: **MISSING**.
- Terms page: **MISSING**.

### Ad-network application readiness checklist
- [x] Original content (50+ articles) — well past Mediavine Journey floor.
- [ ] Privacy policy — **MISSING** (P1-MN-1, blocks all ad-network applications).
- [x] Cookie banner with Consent Mode v2 — textbook implementation.
- [ ] Terms of service — **MISSING** (P1-MN-2, AdSense Program Policies require this).
- [x] About page with author bio — E-E-A-T grade.
- [x] Original photos / no copyrighted-without-license content — AVIF/WebP/JPG triples shipped.
- [ ] Site speed (CWV) — NOT MEASURED (P1-MN-4).
- [x] No prohibited content.

### P1-MN-1 — Privacy policy page MISSING
- Required by every ad network. Required by GDPR + CCPA + state laws the moment GA4 is active (already is).
- Cookie banner currently links to `/about/` ("More on our methodology") instead of `/privacy/` — that link needs a real privacy page.
- Recommended content: cookies set (GA4 `_ga`/`_gid`, Ahrefs Analytics), purpose, opt-out, retention, third parties, CCPA/GDPR contact (`mkml.inc@gmail.com`), no sale of personal info.

### P1-MN-2 — Terms of service / disclaimer page MISSING
- AdSense Program Policies require ToS. Mediavine onboarding asks for one.
- Recommended content: no professional advice / planning estimates only / verify with licensed contractors / no warranty / affiliate disclosure / governing law.

### P1-MN-3 — Footer does not link to privacy or terms
- `Footer.astro` has only Calculators + Trust columns. Ad networks crawl the footer for legal-page links during application review.

### P1-MN-4 — Core Web Vitals not measured
- Mediavine evaluates CWV during approval. Target: Performance ≥ 85, Accessibility ≥ 95, SEO ≥ 95.
- Outstanding from `AUDIT_CLAUDE_2026-05-14` P1-CL-5.
- Logo PNG 1312×241 declared in Footer.astro is heavy for `h-8` rendered (P2-CL-8 from prior audit) — still outstanding.

### P2-MN-1 — `.env.example` doesn't document monetization feature flags
- Neither `PUBLIC_ADS_ENABLED` nor `PUBLIC_AFFILIATES_ENABLED` mentioned. New contributor / future Vercel deploy can't tell how to turn ads on.

### P2-MN-2 — AffiliateModule has no real product cards yet
- Renders only section heading + "Curated picks for the X category" stub when enabled. Flipping the flag today ships an empty section. Order of operations: get Amazon Associates approval → wire cards → flip flag.

### P2-MN-3 — `kind` prop type lists 5 categories; only 3 surfaces use it
- Type union includes `heat_pump` and `load_management` but no calculator imports them. Either trim the type or wire the missing placements.

### P3-MN-1 — Ahrefs Analytics script unconditional (no consent gate)
- `Layout.astro:130` loads `analytics.ahrefs.com/analytics.js` regardless of consent. The comment says "cookieless, no consent gating needed" — matches Ahrefs' published claim. Acceptable under GDPR ePrivacy for legitimately-cookieless analytics; document this in the privacy policy.

### P3-MN-2 — Cookie banner stores consent in `localStorage` with no expiry
- Persists until user clears storage. EU best practice + CCPA say revisit annually. Bump the storage key on policy changes; full TCF only matters if/when programmatic ad exchanges require it.

### What's healthy
- AdSlot gating correct (`import.meta.env.PUBLIC_ADS_ENABLED === 'true'` at build time, returns nothing when false).
- CLS-safe AdSlot (`min-h-[90px] max-w-3xl mx-auto my-8`).
- AffiliateDisclosure pairs 1:1 with AffiliateModule (FTC same-page rule met by construction).
- GA4 + Consent Mode v2: default denied, set inline BEFORE gtag.js loads, `wait_for_update: 500`, re-applied from localStorage. Custom event `calculator_used` debounced via `lastEventRef`.
- **Zero funnel violations** across the source tree. `<form>` only on 6 calculator inputs; no `type="email"` inputs anywhere; no Modernize/Networx/Angi outbound referral links (only citation references on `/sources/`).
- **Zero live affiliate IDs.** Site is positioned to flip the flag the moment Amazon Associates approves; ships zero unapproved affiliate exposure.
- About page E-E-A-T grade.
- Security headers from `vercel.json` present on production.

---

## §7 — Live crawl + local diff

P0: 0 | P1: 2 | P2: 2 | P3: 1

### Crawl stats
- Sitemap URLs: **641** (xmlns correct, slash form)
- Sampled: **60 URLs** across all page types
- 200 OK: **60 / 60**
- 4xx/5xx: **0**
- Live deploy `Last-Modified: 2026-05-27 14:52 UTC` — most recent commit reflected on production.

### P1-LIVE-1 — HTML pages bypass `vercel.json` Cache-Control rule (zero edge cache TTL)
- `vercel.json` declares `Cache-Control: public, max-age=300, s-maxage=3600` only for paths matching `/(.*)\\.(html|xml|txt)`. With `cleanUrls: true`, Vercel serves every page at `/<slug>/` (no `.html` suffix), so the rule **never matches HTML responses**.
- Live HTML returns `Cache-Control: public, max-age=0, must-revalidate` (Vercel default). Every browser revalidates on every request. The intended 5-min edge + 1-hr CDN cache is not in effect for HTML.
- `sitemap.xml` and `robots.txt` DO get the correct cache headers (verified). Static `/_astro/*.css|js` correctly returns `max-age=31536000, immutable`.
- Fix: Change source to `(.*\\..+|.*)` or add a separate rule `{ "source": "/((?!_astro|assets|api).*)", "headers": [{ "key": "Cache-Control", "value": "public, max-age=300, s-maxage=3600" }] }`.

### P1-LIVE-2 — Mini-split has no `[state]` programmatic pages (51 URLs of missed SEO)
- Every other module has a 51-page state template. Mini-split has only the calculator page (`/mini-split-heat-pump-cost-calculator/`) and the guide (`/guides/mini-splits/`).
- `/mini-split-installation-cost-ca/` returns 404 (no template in `src/pages/`).
- Fix: Build `src/pages/mini-split-installation-cost-[state].astro` from one of the existing state templates.

### P2-LIVE-1 — CLAUDE.md inventory drift vs reality
- CLAUDE.md says 642 built pages, 38 calculators, 22 brand pages.
- Live sitemap measured: 641 URLs, 37 `*-cost-calculator/` pages, 22–23 brand pages depending on `ducted-heat-pump-cost` classification.
- Close, but bump the inventory table after recent additions.

### P2-LIVE-2 — Verified no actual drift (false alarm logged in audit for transparency)
- `heat-pump-cost-4-ton/` is live AND in worktree; tonnage roster is 5/5 (1.5/2/3/4/5). No issue.

### P3-LIVE-1 — Homepage H1 wrapped multi-line; naïve grep returns empty
- Not a defect. Browsers and crawlers parse it correctly. Documented in case anyone writes a build-time check that does flat-string H1 matching.

### Diff between worktree and production
- Pages in worktree, not on production: **0**.
- URLs on production, not in worktree: **0** (apparent mismatches like `/rinnai-heat-pump-water-heater-cost/` resolve via `[brand]` dynamic templates reading `data/csv/brand-profiles.csv`).
- Recent-commit deployment verified: `21fbf74` Ahrefs script live; `e02c752` image/title/orphan fixes live; `defffe0` state-template + brand-provenance fixes live.

### What's healthy
- Sitemap 641 URLs, correct xmlns. `sitemap.xml` and `robots.txt` properly cached.
- All 4 security headers visible on production HTML.
- Static asset cache working (`/_astro/*` immutable, year TTL).
- Content-Type charset=utf-8 on all HTML; `application/xml` on sitemap.
- All 60 sampled URLs have title (28–58 chars, all ≤60), meta description (109–184), viewport meta, ≥1 JSON-LD block, H1, footer "Data last refreshed" stamp.
- Zero render errors ("undefined", "[object Object]", "NaN", "null" in body).
- Zero mixed-content (HTTPS everywhere).
- Zero broken image refs.
- Mobile rendering: iPhone UA returns same HTML payload; viewport meta + skip-link + mobile menu markup present; 43+ responsive utility classes.
- Internal navigation 5/5 sampled returned 200; React hydration assets all 200.
- Zero funnel violations spotted.

---

## Prioritized punch list

### P0 (fix immediately)
1. **P0-CQ-1** — Fix wrong 30D credit expiration in `src/pages/guides/ev-tco.astro:58` ("2026-09-30" → "2025-09-30"). One-line fix.

### P1 (fix this week)
2. **P1-MN-1** — Create `src/pages/privacy.astro`.
3. **P1-MN-2** — Create `src/pages/terms.astro`.
4. **P1-MN-3** — Wire privacy + terms links in `Footer.astro`.
5. **P1-LIVE-1** — Fix `vercel.json` HTML Cache-Control regex (clean URLs not matching).
6. **P1-SEO-1** — Pass `breadcrumbs` to `Layout.astro` from all 6 `[state]` templates (306 pages).
7. **P1-SEO-2** — Add `BreadcrumbList` to `/rebates/`.
8. **P1-SEO-3** — Add `WebApplication` schema to sqft / tonnage / brand calculator pages.
9. **P1-CQ-1** — Add hero-level disclaimer to all 6 `[state]` templates.
10. **P1-CQ-2** — Source URLs for unsourced FAQ claims on non-flagship calculators.
11. **P1-CQ-3** — Expand FAQs to 8+ on under-FAQ'd calculators.
12. **P1-CQ-4** — Brand pages: 4 more FAQs each + NREL benchmark URL inline.
13. **P1-A11Y-1** — Promote text-bearing `ink-500` → `ink-600` everywhere.
14. **P1-A11Y-2** — Fix mis-labeled hero alt on `heat-pump-cost-by-state.astro:69-75`.
15. **P1-A11Y-3** — Thicken `:focus-visible` ring or add white halo for brand-50/100 surfaces.
16. **P1-A11Y-4** — Cookie banner buttons + mobile hamburger to 44×44 touch targets.
17. **P1-BUG-1** — Add `*.csv?raw` module declaration to clean tsc baseline.
18. **P1-MN-4** — Run Lighthouse / Core Web Vitals; replace heavy logo PNG with SVG (P2-CL-8 carryover).
19. **P1-CA-1** — Add `source_id` + `last_reviewed` to `state-labor-multipliers.csv`.
20. **P1-LIVE-2** — Build 51 mini-split state pages.

### P2 (fix this month)
21. **P2-CA-1** — Bump CA `hvac_multiplier` 1.38 → 1.45 (or raise `material_mid` for 3-ton heat pump).
22. **P2-CA-2** — Add provenance columns to the other 9 CSVs lacking them.
23. **P2-CA-3** — Zero amount fields on expired rebate rows (defense-in-depth).
24. **P2-CQ-1** — Add byline component with credentials to all 38 calculator pages + 19 unbylined guides; flip Article schema author from Organization to Person.
25. **P2-CQ-2** — Replace "Comprehensive guide" in two guide meta descriptions.
26. **P2-CQ-3** — Replace "Innovative because it fits…" in heat-pump-dryers guide.
27. **P2-CQ-4** — Name / link studies for ductwork (LBNL Aeroseal) and smart-thermostat (Nest white paper + ENERGY STAR spec).
28. **P2-CQ-5** — State pages hero disclaimer (same as P1-CQ-1, separate copy).
29. **P2-CQ-6** — Repeat planning-range disclaimer in calculator-page Quick-answer block.
30. **P2-SEO-1** — Tighten `/rebates/` and `/mitsubishi-heat-pump-cost/` titles.
31. **P2-SEO-2** — Add internal links to `/geothermal-heat-pump-cost-calculator/`.
32. **P2-SEO-3** — Wire `/battery-vs-generator/` into battery + generator calc + guide hubs.
33. **P2-A11Y-1** — Promote Footer column headings from `<h4>` to `<h2>`.
34. **P2-A11Y-2** — Add `prefers-reduced-motion` global CSS block.
35. **P2-A11Y-3** — Hamburger menu: bigger touch target + dynamic aria-label.
36. **P2-MN-1** — Document `PUBLIC_ADS_ENABLED` + `PUBLIC_AFFILIATES_ENABLED` in `.env.example`.
37. **P2-MN-2** — Build real product cards in AffiliateModule before flipping the flag.
38. **P2-MN-3** — Trim `kind` type union to 3, or wire the 2 missing placements.
39. **P2-LIVE-1** — Bump CLAUDE.md inventory counts (37 calculators, 23 brand pages, etc.).

### P3 (polish, deferred)
40. **P3-CA-1** — Calendar a 2026-07-01 task to flip 30C status + past-tense the copy.
41. **P3-CA-2** — Document blended labor rate derivation in `module-labor-rates.csv`.
42. **P3-CQ-1** — Source the "backfeeding illegal in all 50 states" claim.
43. **P3-CQ-2** — Codify or replace dingbat icons.
44. **P3-CQ-3** — Schema.org author: Organization → Person on guides/calculators.
45. **P3-SEO-1** — Bump sqft/tonnage/brand sitemap priority in `build-sitemap.cjs`.
46. **P3-A11Y-1** — Convert ResultPanel `<tr onClick>` to `<button>`-row pattern.
47. **P3-A11Y-2** — Move `→` arrows outside link text (or use `::after`).
48. **P3-MN-1** — Document Ahrefs cookieless analytics in privacy page.
49. **P3-MN-2** — Plan annual consent storage-key bump policy.
50. **P3-LIVE-1** — N/A (false alarm — homepage H1 wrap not a defect).

---

Last reviewed: 2026-05-27.
